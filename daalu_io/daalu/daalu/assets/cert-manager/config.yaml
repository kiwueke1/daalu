cloudflare:
  # If token is empty here, component will fall back to env var CERT_MANAGER_CLOUDFLARE_API_TOKEN.
  api_token: "8ft-e1xSs9aUxGwgXTbkQPVp9q7WSM_0OlzWVBXt"

email: "kaiwueke@gmail.com"

dns_zones:
  - "daalu.io"

cluster_issuers:
  - name: "letsencrypt-staging"
    server: "https://acme-staging-v02.api.letsencrypt.org/directory"
  - name: "letsencrypt-prod"
    server: "https://acme-v02.api.letsencrypt.org/directory"

certificates:
  - name: "daalu-io"
    namespace: "istio-ingress"
    secret_name: "daalu-io-tls"
    dns_names:
      - "daalu.io"
      - "*.daalu.io"
    issuer:
      kind: "ClusterIssuer"
      name: "letsencrypt-prod"

argocd_onboard:
  enabled: true
  # One of:
  # - local_manifest: "argocd-app.yaml"
  # - github_raw_url: "https://api.github.com/repos/<org>/<repo>/contents/apps/cert-manager/cert-manager.yaml"
  local_manifest: ""
  github_raw_url: "https://api.github.com/repos/kiwueke1/argocd-infrastructure-app/contents/apps/cert-manager/cert-manager.yaml"
  # If using github_raw_url, token should come from env var (recommended):
  github_token_env: "GITHUB_TOKEN"

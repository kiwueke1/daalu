# =============================================================================
# Daalu Environment Variables
# =============================================================================
# Copy to .env and fill in your values:
#
#   cp .env.example .env
#   source .env
#   python -m daalu.cli.app deploy cluster-defs/cluster.yaml ...
#
# These variables are expanded in two places:
#
# 1. Inside cluster.yaml / secrets.yaml — any ${VAR} is replaced at load time
# 2. In Python components — read via os.environ.get() at runtime
#
# =============================================================================

# --- Cluster API / Proxmox ---------------------------------------------------
export DAALU_BUILDER_PASSWORD=""
export DAALU_IMAGE_PASSWORD=""
export DAALU_SSH_PUBLIC_KEY=""
export DAALU_PROXMOX_URL=""
export DAALU_PROXMOX_TOKEN=""
export DAALU_PROXMOX_SECRET=""
export DAALU_MGMT_HOST=""
export DAALU_MGMT_USER=""

# --- Keycloak -----------------------------------------------------------------
export DAALU_KEYCLOAK_ADMIN_PASSWORD=""
export DAALU_KEYCLOAK_DB_PASSWORD=""

# --- GitHub (for ArgoCD / private repos) --------------------------------------
export DAALU_GITHUB_TOKEN=""

# --- Monitoring / Thanos ------------------------------------------------------
export DAALU_THANOS_S3_ACCESS_KEY=""
export DAALU_THANOS_S3_SECRET_KEY=""

# --- Database (used by keycloak component, opensearch, etc.) ------------------
export DAALU_MYSQL_ROOT_PASSWORD=""
export DAALU_OPENSEARCH_ADMIN_PASSWORD=""

# User-specific settings file for this repo
.aspect/bazelrc/user.bazelrc
*.log
*__pycache__*

# Don't check in lockfile for now. It is unstable.
# https://github.com/bazelbuild/bazel/issues/19026
# https://github.com/bazelbuild/bazel/issues/19621
# https://github.com/bazelbuild/bazel/issues/19971
# https://github.com/bazelbuild/bazel/issues/20272
# https://github.com/bazelbuild/bazel/issues/20369
MODULE.bazel.lock

# Symlinks created by Bazel into the output tree
/bazel-*

# Virtual environments
*.venv/
*venv/
.venv/
daalu_venv/

# Generated artifacts (contain interpolated secrets)
artifacts/

# Helm charts vendored into assets — download separately (see README)
# values.yaml files within each asset directory ARE tracked
assets/*/charts


# ==========================================
# Secrets — NEVER commit real credentials
# ==========================================
cloud-config/secrets.yaml
secrets.yaml
cluster-defs/cluster.yaml
!cluster-defs/cluster.yaml.example
!secrets.yaml.example
.env
.env.*
!.env.example
*.pem
*.key
id_rsa
id_ed25519

# Inventory files (contain IPs, hostnames, and credentials)
cloud-config/inventory/hosts.ini
cloud-config/inventory/openstack_hosts.ini
cloud-config/inventory/group_vars/all/secrets.yml
!cloud-config/inventory/*.example

# Kubeconfig files (contain cluster certs and keys)
cloud-config/*.kubeconfig
*.kubeconfig

# Logs (may contain sensitive deployment output)
logs/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Third-party / vendored
github.com/aspect-build/
helm-charts/
helm-values/
